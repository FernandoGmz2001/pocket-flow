# Pocket Flow

PWA móvil de finanzas personales (React + Vite + TypeScript).

## Scripts

```bash
npm install
npm run dev
```

La app queda instalable en la pantalla de inicio (manifiesto + service worker en `npm run build`).
